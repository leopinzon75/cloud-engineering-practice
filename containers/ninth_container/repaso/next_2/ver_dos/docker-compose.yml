version: '3.8'

services:
  minio:
    image: minio/minio:latest
    container_name: minio_ver_dos
    ports:
      - "9000:9000"
      - "9003:9001"
    environment:
      MINIO_ROOT_USER: minio_admin
      MINIO_ROOT_PASSWORD: minio_password
    command: server /data --console-address ":9001"
